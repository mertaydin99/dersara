 // Random image selector to display as a background image
 /*
var imageNames = [
  'viyolin.jpeg'
];
var arrayLenght = imageNames.length;
var selectedImage = imageNames[ Math.floor(Math.random() * arrayLenght)];
// Get the image name in order to add to image's alt attribute
var imageName = selectedImage.substring(0, selectedImage.indexOf('.'));
var captName = imageName[0].toUpperCase()  + imageName.slice(1); // Capitalize first letter of the name of the lesson
var alt = captName + ' ' + 'Dersi';
var imageUrl = 'images/' + selectedImage ;
var $backgroundImage = $("#background-img");

$( window ).on("load", function() 
{
  // Add the background image
  $backgroundImage.attr({ src: imageUrl, alt: alt });
});

*/